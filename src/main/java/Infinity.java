public class Infinity extends Car{
    public Infinity(int year, String color, String bodyType, String transmissionType, String driveType, String engineType) {
        super(year, color, bodyType, transmissionType, driveType, engineType);
    }
}
