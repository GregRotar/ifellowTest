public class Fiat extends Car{
    public Fiat(int year, String color, String bodyType, String transmissionType, String driveType, String engineType) {
        super(year, color, bodyType, transmissionType, driveType, engineType);
    }
}
