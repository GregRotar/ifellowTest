public class GMC extends Car{

    public GMC(int year, String color, String bodyType, String transmissionType, String driveType, String engineType) {
        super(year, color, bodyType, transmissionType, driveType, engineType);
    }
}
